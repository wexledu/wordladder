# Offline Word Ladder

This directory contains a lightweight offline implementation of the Word Ladder game.
Open `index.html` in a modern browser to play. No build steps or server are required.

The game data lives in `wordlist.json`. Teachers can extend the dictionary or add
levels by editing this file.
